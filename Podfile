# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

target 'Test_iTestFramework' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'iTestFramework'

  # Pods for Test_iTestFramework

  target 'Test_iTestFrameworkTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Test_iTestFrameworkUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
